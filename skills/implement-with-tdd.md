# Implement with TDD

When the active Project Rules require TDD, express the intended behavior in a failing test, implement the smallest change that satisfies it, and refactor only while verification remains green. This Skill is an optional strategy, not a universal Scaffold requirement.
